# Deprecated runtime

This directory is retained only as historical reference. It is not deployed
and must not be used as an API service.

The canonical network-aware backend is `../base_mainnet`. Its strict request
network contract routes Base Mainnet and Arc Testnet without sharing targets,
native-asset assumptions, or transaction plans.
