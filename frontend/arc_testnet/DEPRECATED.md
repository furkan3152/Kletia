# Deprecated runtime

This directory is retained only as historical reference. It is not deployed
and must not be used for Base or Arc runtime behavior.

The canonical network-aware frontend is `../base_mainnet`. It selects the
complete Base Mainnet or Arc Testnet manifest at runtime and does not use mock
quotes or fallback analytics.
