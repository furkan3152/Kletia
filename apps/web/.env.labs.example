# Build-only Policy V2 lab artifacts. Core Payment Center builds do not stage
# these files. URLs must be immutable public HTTPS resources without secrets.
STELLAR_POLICY_V2_PROVER_WASM_RELEASE_URL=
STELLAR_POLICY_V2_PROVING_KEY_RELEASE_URL=
